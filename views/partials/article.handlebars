<div data-id="{{_id}}" class="card mb-2">
    <div class="card-header">
        <div class="row">
            <div class="col-10">
                <img src={{img}} class="float-left">
                <a href={{link}} target="_blank" class="text-white">
                    <h2>{{title}}</h2>
                </a>
            </div>
            <div class="col-2">
                <a data-id="{{_id}}" class="btn btn-success save float-right">Save Article</a>
            </div>
        </div>
    </div>
</div>
</div>